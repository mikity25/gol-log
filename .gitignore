# ----------------------------------------------------
# 秘密情報・環境変数（絶対にGitHubに上げない）
# ----------------------------------------------------
/.env*
/config/*.key

# ----------------------------------------------------
# ログ・一時ファイル（作業中のゴミ箱）
# ----------------------------------------------------
/.bundle
/log/*
/tmp/*
!/log/.keep
!/tmp/.keep

# プロセスID管理用の一時ファイル
/tmp/pids/*
!/tmp/pids/
!/tmp/pids/.keep

# ----------------------------------------------------
# アップロード画像・ストレージ
# ----------------------------------------------------
/storage/*
!/storage/.keep
/tmp/storage/*
!/tmp/storage/
!/tmp/storage/.keep

# ----------------------------------------------------
# 自動生成されるフロントエンド資産（JS / CSS / Node）
# ----------------------------------------------------
/public/assets
/app/assets/builds/*
!/app/assets/builds/.keep
/node_modules

# ----------------------------------------------------
# テスト結果・カバレッジレポート（SimpleCov）
# ----------------------------------------------------
/coverage/